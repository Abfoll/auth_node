# Copy this file to .env and fill in the real values (do NOT commit .env)
# Example for MongoDB Atlas
# Replace <db_password> with your real password (URL-encode if it contains special characters)
MONGO_URI="mongodb+srv://abeni:<#Ab02Tk..>@cluster0.amyvm9g.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"
SESSION_SECRET='replace_with_a_long_random_string'
